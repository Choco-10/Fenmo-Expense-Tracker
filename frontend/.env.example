# Default: use deployed backend (Render)
VITE_API_BASE_URL=https://fenmo-expense-tracker-5d3b.onrender.com

# For local testing against your local backend, create a .env.local with:
# VITE_API_BASE_URL=http://localhost:5000

# Note: Vite loads .env, .env.local, .env.development, etc. Do NOT commit .env.local